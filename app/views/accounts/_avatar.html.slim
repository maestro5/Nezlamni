- if request.path.include?('edit')
  = link_to image_tag(avatar(@account), alt: 'avatar', class: 'img-rounded'), account_images_path(@account), class: 'avatar'
- else
  = link_to image_tag(avatar(@account), alt: 'avatar', class: 'img-rounded'), account_path(@account), class: 'avatar'
